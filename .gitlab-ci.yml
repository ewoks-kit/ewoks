include:
  - project: workflow/ewoksadmin/ewoksci
    file: .gitlab-ci-template.yml

variables:
  WARNING1: "ignore::DeprecationWarning:jupyter_client.session"
  IGNORE_WARNINGS: "-W ${WARNING1}"

flake8_nb:
  extends: .flake8_nb

test-3.8_glx:
  extends: .test-3.8_glx
  variables:
    PIP_INSTALL_OPTIONS: '--pre git+https://github.com/woutdenolf/papermill.git@fix_hanging_execute'
    JUPYTER_PLATFORM_DIRS: 1
    PYTEST_WARNINGS: "-W error ${IGNORE_WARNINGS}"

build_sdist:
  extends: .build_sdist

test_sdist-3.7_glx:
  extends: .test_sdist-3.7_glx
  script:
    - !reference [.test_sdist-3.7_glx, script]
    - python -m pip install ewokscore[test]
    - python -m pytest -v --pyargs ewokscore.tests
    - python -m pytest -v --pyargs ewoksppf.tests
    - python -m pytest -v --pyargs ewoksdask.tests
    - python -m pytest -v --pyargs ewoksorange.tests
  variables:
    PIP_INSTALL_OPTIONS: '--pre git+https://github.com/woutdenolf/papermill.git@fix_hanging_execute'
    JUPYTER_PLATFORM_DIRS: 1
    PYTEST_WARNINGS: "-W error ${IGNORE_WARNINGS}"
  timeout: 15m

test_sdist-3.8_glx-win:
  extends: .test_sdist-3.8_glx-win
  script:
    - !reference [.test_sdist-3.8_glx-win, script]
    - python -m pip install ewokscore[test]
    - python -m pytest -v --pyargs ewokscore.tests
    - python -m pytest -v --pyargs ewoksppf.tests
    - python -m pytest -v --pyargs ewoksdask.tests
    #- python -m pytest -v --pyargs ewoksorange.tests
  variables:
    JUPYTER_PLATFORM_DIRS: 1
    PYTEST_WARNINGS: "-W error ${IGNORE_WARNINGS}"
  timeout: 15m

test_sdist-3.9_glx:
  extends: .test_sdist-3.9_glx
  script:
    - !reference [.test_sdist-3.9_glx, script]
    - python -m pip install ewokscore[test]
    - python -m pytest -v --pyargs ewokscore.tests
    - python -m pytest -v --pyargs ewoksppf.tests
    - python -m pytest -v --pyargs ewoksdask.tests
    - python -m pytest -v --pyargs ewoksorange.tests
  variables:
    JUPYTER_PLATFORM_DIRS: 1
    PYTEST_WARNINGS: "-W error ${IGNORE_WARNINGS}"
  timeout: 15m

test_sdist-3.10_glx:
  extends: .test_sdist-3.10_glx
  script:
    - !reference [.test_sdist-3.10_glx, script]
    - python -m pip install ewokscore[test]
    - python -m pytest -v --pyargs ewokscore.tests
    - python -m pytest -v --pyargs ewoksppf.tests
    - python -m pytest -v --pyargs ewoksdask.tests
    - python -m pytest -v --pyargs ewoksorange.tests
  variables:
    JUPYTER_PLATFORM_DIRS: 1
    PYTEST_WARNINGS: "-W error ${IGNORE_WARNINGS}"
  timeout: 15m

test_sdist-3.11_glx:
  extends: .test_sdist-3.11_glx
  script:
    - !reference [.test_sdist-3.11_glx, script]
    - python -m pip install ewokscore[test]
    - python -m pytest -v --pyargs ewokscore.tests
    - python -m pytest -v --pyargs ewoksppf.tests
    - python -m pytest -v --pyargs ewoksdask.tests
    - python -m pytest -v --pyargs ewoksorange.tests
  variables:
    JUPYTER_PLATFORM_DIRS: 1
    PYTEST_WARNINGS: "-W error ${IGNORE_WARNINGS}"
  timeout: 15m

build_doc:
  extends: .build_doc

pages:
  extends: .pages

assets:
  extends: .assets
