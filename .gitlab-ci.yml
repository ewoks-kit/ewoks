include:
  .gitlab-ci-template.yml

flake8_nb:
  extends: ".flake8_nb"

test-3.6:
  extends: ".test-3.6_glx"

deploy_docs:
  extends: ".sphinx_and_artifacts"
  script: # TODO: image needs pandoc
    - apt-get update
    - apt-get -y install pandoc
    - python setup.py build_sphinx

pages:
  extends: ".pages"
